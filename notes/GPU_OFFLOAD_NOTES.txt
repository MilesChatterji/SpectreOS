GPU Offloading Configuration - Notes and Future Work
====================================================
Date: 2024-11-26 (Updated - RESOLVED)
System: NixOS with Niri Wayland Compositor
Hardware: AMD Radeon 890M iGPU + NVIDIA RTX 4050 dGPU

CURRENT STATUS - ✅ RESOLVED
----------------------------
- ✅ AMD iGPU (Radeon 890M) is driving the display and compositor
- ✅ NVIDIA GPU is idle when not needed (fans off = good power efficiency)
- ✅ Battery drain is at expected levels for system specifications
- ✅ Demanding apps (e.g., DaVinci Resolve) automatically use NVIDIA when needed
- ✅ nvidia-offload wrapper still available for manual GPU selection
- ✅ Power consumption optimized - NVIDIA draws minimal power when idle

WHAT'S WORKING
--------------
✓ Proprietary NVIDIA drivers installed and configured
✓ NVIDIA GPU power management enabled (powers down when idle)
✓ Environment variables set for AMD-first GPU selection
✓ nvidia-offload wrapper available for forcing NVIDIA usage
✓ amd-only wrapper available for forcing AMD usage
✓ System-wide configuration in gpu-offload.nix
✓ Niri session wrapper with AMD environment variables
✓ Niri explicitly configured to use AMD render device (renderD128)
✓ Xwayland configured to use AMD via GBM_BACKEND
✓ Display rendering working correctly on AMD iGPU

CONFIGURATION FILES
-------------------
- /etc/nixos/gpu-offload.nix: System-wide GPU configuration
- /etc/nixos/niri.nix: Niri-specific GPU configuration with AMD wrapper
- /etc/nixos/configuration.nix: Imports both above files

ENVIRONMENT VARIABLES SET
-------------------------
- WLR_DRM_DEVICES=/dev/dri/card1 ✅ Working - Forces Niri to use AMD card
- WLR_DRM_NO_MODIFIERS=1
- __GLX_VENDOR_LIBRARY_NAME=mesa
- DRI_PRIME=0
- __NV_PRIME_RENDER_OFFLOAD=0
- GBM_BACKEND=/dev/dri/renderD128 ✅ Working - Forces Xwayland to use AMD
- MESA_LOADER_DRIVER_OVERRIDE=radeonsi

GPU DEVICE MAPPING (CORRECTED)
-------------------------------
⚠️ IMPORTANT: The initial mapping was incorrect!
- /dev/dri/card0: NVIDIA RTX 4050 (renderD129) ⚠️ renderD129 is NVIDIA
- /dev/dri/card1: AMD Radeon 890M iGPU (renderD128) ⚠️ renderD128 is AMD

The key fix was correcting the render device mapping:
- renderD128 = AMD iGPU (correct device for compositor)
- renderD129 = NVIDIA dGPU (should NOT be used for compositor)

SOLUTION IMPLEMENTED
--------------------
✅ The issue was resolved by:

1. **Correcting Device Mapping**: Identified that renderD128 = AMD, renderD129 = NVIDIA
   (Initial assumption was backwards)

2. **Niri Config File**: Added explicit render device configuration in ~/.config/niri/config.kdl:
   ```kdl
   debug {
       render-drm-device "/dev/dri/renderD128"
   }
   ```
   This forces Niri to use the AMD render device instead of auto-detecting.

3. **Environment Variables**: Updated all GBM_BACKEND references from renderD129 to renderD128
   - Updated in niri.nix wrapper script
   - Updated in desktop entry
   - Updated in systemd user environment

4. **NVIDIA Modesetting**: Set `hardware.nvidia.modesetting.enable = false` in gpu-offload.nix
   - Prevents NVIDIA from being initialized for display
   - NVIDIA can still be used for compute/rendering via offload
   - AMD iGPU handles all display output

5. **Verification**: Confirmed via logs and environment checks:
   - Niri logs show: "using as the render node: /dev/dri/renderD128"
   - Environment variables correctly set in Niri and Xwayland processes
   - NVIDIA GPU is idle (0% utilization, minimal power draw)
   - Display is working correctly

COMMANDS FOR MONITORING
-----------------------
# Check NVIDIA GPU usage
nvidia-smi pmon -c 1

# Check NVIDIA GPU power
nvidia-smi --query-gpu=power.draw --format=csv,noheader

# Check systemd user environment
systemctl --user show-environment | grep -E "(WLR|GBM|MESA|DRI)"

# Check process environment
ps e -p $(pgrep -f niri-session | head -1) | tr ' ' '\n' | grep -E "(WLR|GBM|MESA|DRI)"

# Check active GPU processes
nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv,noheader

WRAPPERS AVAILABLE
------------------
- nvidia-offload <command>: Force application to use NVIDIA GPU
- amd-only <command>: Force application to use AMD iGPU only

CURRENT STATE - ✅ OPTIMAL
---------------------------
✅ AMD iGPU is driving the display and compositor
✅ NVIDIA GPU is idle when not needed (fans off)
✅ Battery drain is at expected levels
✅ Demanding apps automatically use NVIDIA when needed
✅ System is functional and stable
✅ Power consumption optimized

FINAL CONFIGURATION FILES
--------------------------
- ~/.config/niri/config.kdl: Contains `debug { render-drm-device "/dev/dri/renderD128" }`
- /etc/nixos/niri.nix: Wrapper script with AMD environment variables
- /etc/nixos/gpu-offload.nix: NVIDIA modesetting disabled, power management enabled

HARDWARE-SPECIFIC VALUES (FOR THIS SYSTEM)
-------------------------------------------
⚠️ THESE VALUES ARE HARDWARE-SPECIFIC AND MUST BE DETECTED/CONFIGURED PER SYSTEM:

Hardware Model:
- AMD Radeon 890M iGPU (integrated)
- NVIDIA RTX 4050 dGPU (discrete)

Device Paths (HARDWARE-SPECIFIC):
- /dev/dri/card0: NVIDIA RTX 4050 (renderD129)  ⚠️ May differ on other systems
- /dev/dri/card1: AMD Radeon 890M iGPU (renderD128)  ⚠️ May differ on other systems
- ⚠️ CRITICAL: renderD128 = AMD, renderD129 = NVIDIA (was initially reversed!)

Environment Variable Values (HARDWARE-SPECIFIC):
- WLR_DRM_DEVICES=/dev/dri/card1  ⚠️ Must detect which card is iGPU
- GBM_BACKEND=/dev/dri/renderD128  ⚠️ Must detect AMD render node (renderD128, NOT renderD129!)
- MESA_LOADER_DRIVER_OVERRIDE=radeonsi  ⚠️ AMD-specific, Intel would be "i965" or "iris"
- Niri config: render-drm-device "/dev/dri/renderD128"  ⚠️ Must use AMD render node

GENERALIZABLE PATTERNS (CAN BE REUSED)
---------------------------------------
✓ Pattern: iGPU-first configuration with dGPU offload capability
✓ Pattern: Environment variable setup for GPU selection
✓ Pattern: Wrapper scripts for forcing GPU usage (nvidia-offload, amd-only)
✓ Pattern: System-wide GPU configuration in separate module
✓ Pattern: Compositor-specific wrapper with environment variables
✓ Pattern: Power management configuration for NVIDIA drivers

HOW TO GENERALIZE FOR DISTRIBUTION
-----------------------------------
For a general distribution, the following needs to be made configurable:

1. GPU Detection:
   - Auto-detect which /dev/dri/card* is iGPU vs dGPU
   - Auto-detect render node numbers (renderD*)
   - Support Intel iGPU (iris/i965) in addition to AMD (radeonsi)
   - Support different NVIDIA GPU models

2. Configuration Options:
   - Make GPU device paths configurable (or auto-detected)
   - Make Mesa driver override configurable based on detected iGPU vendor
   - Allow user to choose default GPU (iGPU vs dGPU)
   - Allow user to enable/disable GPU offload wrappers

3. Files to Generalize:
   - gpu-offload.nix: Make device paths and driver names configurable
   - niri.nix: Make card numbers and render nodes configurable
   - Wrapper scripts: Use detected values instead of hardcoded paths

4. Detection Script Needed:
   - Script to identify iGPU vs dGPU
   - Script to find correct render node for iGPU
   - Script to determine Mesa driver name based on iGPU vendor

5. What Can Be Removed for General Distribution:
   - Hardcoded card numbers (card0, card1)
   - Hardcoded render node numbers (renderD128, renderD129)
   - Hardcoded GPU model names (Radeon 890M, RTX 4050)
   - Hardcoded Mesa driver (radeonsi) - should detect Intel vs AMD
   - Power consumption values specific to this hardware
   - ⚠️ IMPORTANT: Must auto-detect which render node corresponds to which GPU!
     The mapping (renderD128=AMD, renderD129=NVIDIA) is hardware-specific

6. What Should Stay:
   - General GPU offload pattern
   - Environment variable approach
   - Wrapper script concept
   - Power management configuration
   - Module structure (separate files for GPU config)

EXAMPLE GENERALIZED CONFIGURATION
----------------------------------
Instead of:
  WLR_DRM_DEVICES=/dev/dri/card1
  GBM_BACKEND=/dev/dri/renderD128  # ⚠️ Note: renderD128 is AMD, not renderD129!
  MESA_LOADER_DRIVER_OVERRIDE=radeonsi
  Niri config: render-drm-device "/dev/dri/renderD128"

Use:
  WLR_DRM_DEVICES=${config.hardware.gpu.igpu.device}
  GBM_BACKEND=${config.hardware.gpu.igpu.renderNode}  # Must detect AMD render node
  MESA_LOADER_DRIVER_OVERRIDE=${config.hardware.gpu.igpu.mesaDriver}
  Niri config: render-drm-device "${config.hardware.gpu.igpu.renderNode}"

Where these values are auto-detected or user-configured.
⚠️ CRITICAL: The render node detection must correctly identify which render node
belongs to the iGPU, as the numbering (renderD128 vs renderD129) is not consistent
across systems and depends on initialization order.
